package kh.java.oop.access.modifier.class_;
/**
 * 접근제한자 Access Modifier
 * 
 * field / method 
 * 1. public - 다른 패키지, 다른 클래스 모두 가능 !!!
 * 2. protected - 같은 패키지 가능 , 다른 패키지라도 상속관계는 접근가능
 * 3. default(작성금지) 그냥 비워두는것 - 같은 패키지 에서만 접근 가능
 * 4. private - 같은 클래스 내에서만 접근 가능
 *
 *class // class 는 public  만 사용하면됨 default 는 보통 사용안함
 *1. public - 다른 패키지, 다른 클래스 모두 가능 !!!
 *2. default(작성금지) 그냥 비워두는것 - 같은 패키지 에서만 접근 가능
 *
 */

public class Apple {
	
}
/**
 * 아래는
 * defalut class
 * package
 * 
 * 객체지향 프로그래밍 : 완성된 클래스를 모듈로써(도구로) 가져다 쓰도록 제공하는것.
 *
 */
class PineApple {
	
}
